<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Developer Blog</title>
      <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/css/jass.css" />
    <link rel="stylesheet" href="/css/style.css" />
  </head>
  <body>
    <div class="flex-column justify-space-around">
      <header class="text-3xl display-flex justify-space-between align-center p-2 bg-black">
        <h1>
          <span role="img" aria-label="tech">👨‍💻</span>
          <a class="text-teal-300" href="/">Developer Blog</a>
        </h1>
        <nav>
          <!-- Conditionally render login or logout links -->
          {{#if logged_in}}
            <a class="text-teal-300" href="/profile">profile</a>
            |
            <button class=" text-teal-300 no-button" id="logout">logout</button>
          {{else}}
            <a href="/login">login</a>
          {{/if}}
        </nav>
      </header>
      <main class="container container-fluid mt-5">
        <!-- Render the sub layout -->
        {{{body}}}
      </main>
      <footer>

      </footer>
    </div>

    <!-- Render script for logged in users only -->
    {{#if logged_in}}
      <script src="/js/logout.js"></script>
    {{/if}}
  </body>

</html>